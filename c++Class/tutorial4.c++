#include <iostream>
#include <string>
using namespace std;

int main() {
    int n, x;
    cin >> n;
    cin >> x;
    cout << n << endl;
    cout << x;
    return(0);
}
/* 
 endl  adds a new line 
 cout << cin.fail() tells  if there is a failure
 cin.clear() removes error in our program
 cin.ignore(1000, '\n')
*/