#include <iostream>
#include <string>
using namespace std;

int main() {
    int x = 8;
    float y = 9.986;
    int sum =  x + y;
    int sub = x - y;
    float div = x / y;
    float mul = x * y;
    cout << sub << endl;
    cout << sum << endl;
    cout << div << endl;
    cout << mul << endl;

}
/*
+, -, /, *, ++(increment adds 1 to the integer),--(decrement subtracts 1 from the integer), +=, -=, *=, /=
*/
